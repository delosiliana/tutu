<h1><%= t('.title') %></h1>

<%= render 'form', train: @train %>

<%= link_to t('common.show'),[:admin, @train], class: 'btn btn-info'  %> |
<%= link_to t('common.back'), admin_trains_path, class: 'btn btn-secondary' %>
