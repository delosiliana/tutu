<h1><%= t('.title') %></h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th><%= t('.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('common.new'), new_admin_train_path, class: 'btn btn-primary' %>
<%= link_to t('common.back'), admin_panel_index_path, class: 'btn btn-secondary' %>

