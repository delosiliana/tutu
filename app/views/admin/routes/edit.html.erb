<h1><%= t('.title') %></h1>

<%= render 'form', route: @route, hidden: false %>

<%= link_to t('common.show'), [:admin, @route], class: 'btn btn-info' %> |
<%= link_to t('common.back'), admin_routes_path, class: 'btn btn-secondary' %>
