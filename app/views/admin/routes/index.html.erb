<h1>Список маршрутов</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Имя</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @routes %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить новый маршрут', new_admin_route_path, class: 'btn btn-primary' %>
<%= link_to t('common.back'), admin_panel_index_path, class: 'btn btn-secondary' %>
